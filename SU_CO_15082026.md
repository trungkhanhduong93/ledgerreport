# SỰ CỐ 15/08/2026 — LedgerReport bị phá vỡ và quá trình dựng lại

> Đọc file này **trước khi** định "khôi phục", "dọn dẹp" hay "viết lại" bất cứ thứ gì trong project.
> Đây không phải nhật ký. Đây là danh sách những cách cụ thể mà project này đã bị làm hỏng,
> và cách phát hiện ra từng cái. Nguồn sự thật về kiến trúc vẫn là [CLAUDE.md](CLAUDE.md).

---

## 1. Chuyện gì đã xảy ra

Một agent AI được giao "khôi phục lại các báo cáo BC001–BC014". Nó **nối 505 dòng vào cuối
`server.py`** mà không đọc phần đã có. Commit `b90bdc9`, thông điệp nghe rất ổn:
*"feat: restore all 14 standard reports BC001-BC014 and release v1.8.0"*.

Kết quả người dùng thấy: **bấm Xem báo cáo là văng ra khỏi phần mềm**, BC001–BC004 và BC009–BC011
không mở được. Build vẫn xanh. `ast.parse` vẫn xanh. EXE vẫn đóng gói thành công.

Tổng cộng **7 lỗi** phải sửa qua 4 bản phát hành (v1.8.1 → v1.8.4), cộng 1 lỗi kiến trúc về phạm vi
dữ liệu làm sai số liệu kế toán mà **không ai nhìn thấy** vì báo cáo vẫn mở ra bình thường.

---

## 2. Bảy lỗi — triệu chứng, nguyên nhân, cách phát hiện

| # | Triệu chứng người dùng | Nguyên nhân thật | Phát hiện bằng |
|---|---|---|---|
| 1 | "Bấm là văng ra khỏi phần mềm" | `session["logged_in"]` được **đọc ở 3 endpoint nhưng không bao giờ được gán** → luôn trả 401 → frontend `setIsLoggedIn(false)` | `grep -n "logged_in" server.py` — thấy chỗ đọc mà không có chỗ gán |
| 2 | BC009/BC010 lỗi 500 | `_calc_results` **định nghĩa 2 lần**; bản nối thêm ở cuối che mất bản trên → `KeyError: 'expense_class'` | Quét trùng tên hàm bằng `ast` |
| 3 | BC002/BC004 lỗi 500 | `SELECT L.EXPENSE_NAME` — **LEDGER không có cột này** (phải JOIN `DM_EXPENSE` lấy `E.EXPENSE_NAME`) | Đối chiếu với bản git đã chạy thật |
| 4 | BC011 lỗi 500 | Gọi `_compute_cdkt()` — **hàm đã bị mất**, agent không viết lại | `test_client` chạy thật → `NameError` |
| 5 | Nút "Xuất toàn bộ CSV" của BC014 báo lỗi | Frontend đổi mã BC013→BC014 nhưng backend còn khoá `"BC013"` | Đối chiếu mã BC hai đầu |
| 6 | BC001/BC003 mất các dòng tổng | Điều kiện lọc dòng bằng 0 bỏ mất danh sách giữ lại `09..18` (LN trước thuế, LN sau thuế…) | Diff mảng `indicators` với bản git |
| 7 | Tab "Doanh thu chờ phân bổ" chết hoàn toàn | `SELECT RECEIVE_DATE` — cột **không tồn tại**. Tab này vốn của LedgerStudio, bị copy nhầm sang | Chạy thật + introspect `INFORMATION_SCHEMA.COLUMNS` |

### Lỗi thứ 8 — nguy hiểm nhất vì KHÔNG có triệu chứng

**BC005, BC006, BC007, BC008, BC013 và 7 tab danh sách gộp cả đơn vị ngoài cây `'00'`**, trong khi
BC001–BC004, BC009–BC011, BC014 thì loại. Báo cáo vẫn mở ra, vẫn có số, nhìn không thấy gì bất thường.

Hệ quả thật: **Bảng cân đối kế toán không cân** — lệch `3.252.634.439`, và không báo cáo nào tie
được với báo cáo nào.

Chỉ lộ ra khi ép hai câu hỏi:
- *Tổng tài sản có bằng tổng nguồn vốn không?* → không
- *Tiền trên CĐKT có bằng tiền cuối kỳ trên LCTT không?* → không

Sau khi thống nhất bộ lọc: `270 = 440 = 165.309.773.349`, và Tiền = `8.217.295.888` ở cả hai báo cáo.

---

## 3. Bốn bài học — đây mới là phần cần nhớ

### 3.1. "Build xanh" không chứng minh gì cả

Cả 7 lỗi trên đều **lọt qua** `ast.parse`, `@babel/parser`, và PyInstaller. Bốn trong bảy lỗi chỉ lộ
ra khi **gọi API thật trên DB thật**. Lỗi thứ 8 chỉ lộ khi **đối chiếu số liệu giữa các báo cáo**.

Thang đo bắt buộc ghi rõ trong mọi báo cáo bàn giao:

```
M1 dịch được  <  M2 test_client pass  <  M3 chạy EXE thật  <  M4 số liệu khớp nguồn sự thật
```

**Báo cáo kế toán chỉ được bàn giao ở M4.** Đạt M1 mà nói "đã xong" là nói dối.

### 3.2. Mất hàm thì TÌM, đừng VIẾT LẠI

Khi thấy `_compute_cdkt` không tồn tại, tôi **tự viết lại** nó. Bản tự viết:
- lọc đơn vị bằng `ORGANIZATION_ID IN` thay vì `_org_filter_sql` → **ra số khác**
- thiếu hẳn phần tách `1311/1312` và `421A/421B`

Trong khi **bản gốc vẫn còn nguyên** trong `git show b6553f6:server.py`. Mất 1 vòng phát hành để sửa.

```bash
cd ledgerreport
git show b6553f6:server.py > /tmp/head_server.py   # bản 04/07/2026, đã chạy thật, đủ BC001–BC013
```

Rồi so **danh sách hàm + route** để biết cái gì đã mất:

```python
python -c "
import ast
def names(p):
    t=ast.parse(open(p,encoding='utf-8').read())
    fn={n.name for n in t.body if isinstance(n,ast.FunctionDef)}
    rt={d.args[0].value for n in ast.walk(t) if isinstance(n,ast.FunctionDef)
        for d in n.decorator_list
        if isinstance(d,ast.Call) and getattr(d.func,'attr','')=='route' and d.args}
    return fn,rt
hf,hr=names('/tmp/head_server.py'); cf,cr=names('server.py')
print('HAM BI MAT  :', sorted(hf-cf) or '(khong)')
print('ROUTE BI MAT:', sorted(hr-cr) or '(khong)')
"
```

### 3.3. Sửa bộ lọc dùng chung ⇒ phải rà **mọi** chỗ, và đếm lại dấu `?`

Thống nhất `_org_filter_sql` chạm 29 chỗ. Riêng việc đó **tự gây ra 2 lỗi 500 mới**, cùng một kiểu:

> `The SQL contains 2 parameter markers, but 3 parameters were supplied`

Vì khi không chọn đơn vị, `org_ids` **rỗng** nhưng `org_params` lại **có 1 phần tử** (`'66'`).
Mọi dòng còn dựng params theo `org_ids` đều lệch.

```bash
# Sau khi đổi bộ lọc, luôn quét lại:
grep -n "list(org_ids)\|+ org_ids" server.py     # phải rỗng
```

Và **chạy thử từng đường**, kể cả đường xuất CSV — lỗi BC007 CSV chỉ lộ khi thật sự gọi nó.

### 3.4. Luật nghiệp vụ của khách này ≠ luật của khách khác

"Loại đơn vị ngoài cây `'00'`" là luật **riêng của IACC_CHULONG**. `_get_external_org_ids()` suy ra
danh sách bằng cách xem đơn vị nào không lần được về mã `'00'`.

⚠️ **DB không có đơn vị gốc `'00'` thì MỌI đơn vị bị coi là "ngoài cây"** → `NOT IN (tất cả)` →
**mọi báo cáo trả 0 dòng, không một lỗi nào**. Đã chèn chốt an toàn: không tìm thấy `'00'` thì
không lọc gì và ghi cảnh báo vào log.

---

## 4. Quy trình bắt buộc khi được giao "khôi phục / dọn dẹp / viết lại"

1. **Đọc [CLAUDE.md](CLAUDE.md) mục 0** — xác định đang ở LedgerReport hay LedgerStudio.
2. **Lấy bản đối chiếu** `git show b6553f6:server.py`, so danh sách hàm + route (mục 3.2).
3. **Cấm nối code vào cuối file.** Trước khi thêm hàm, quét trùng tên:
   ```bash
   python -c "import ast,collections;t=ast.parse(open('server.py',encoding='utf-8').read());c=collections.Counter(n.name for n in t.body if isinstance(n,ast.FunctionDef));print({k:v for k,v in c.items() if v>1} or 'khong trung')"
   ```
4. **Chạy `test_client` trên DB thật**, không chỉ compile. Mọi endpoint, kể cả xuất CSV.
5. **Ép các đẳng thức kế toán** — đây là bước duy nhất bắt được lỗi loại 8:
   - BC005: `mã 270 == mã 440`
   - BC006: `Nợ == Có` ở cả dư đầu, phát sinh, dư cuối
   - BC001 `mã 13` == BC009/BC010 `mã 01` == BC011 `mã 01`
   - BC005 `mã 110` == BC009 `mã 70`
   - BC002: tổng các công việc == tổng chung
6. **Chạy skill `pre-push-qa`** trước khi commit.
7. **Đồng bộ bằng `Sync-And-Backup.ps1`**, không copy tay.

---

## 5. Những con số dùng làm mốc đối chiếu

Kỳ **01/07–31/07/2026**, mặc định (chỉ đơn vị trong cây `'00'`), DB `IACC_CHULONG`:

| Chỉ tiêu | Giá trị |
|---|---|
| BC005 Tổng tài sản = Tổng nguồn vốn | `165.309.773.349` |
| BC005 mã 110 Tiền = BC009 mã 70 | `8.217.295.888` |
| BC001/BC002/BC009/BC010/BC011 — LN trước thuế | `4.054.883.218` |
| BC006 tổng phát sinh Nợ = Có | `331.164.100.304` |

Luỹ kế **01/01–31/07/2026**: LN trước thuế `24.098.362.724`, doanh thu thuần `238.964.073.603`,
tổng phát sinh `2.262.469.808.105`.

Bối cảnh dữ liệu: `LEDGER` 18.516.886 dòng (02/12/2025 → 01/10/2026), 83 đơn vị có phát sinh,
`BALANCE_VIEW` **trống 0 dòng** (nên mọi số dư đầu kỳ phải dồn từ LEDGER — đó là lý do BC005/BC011 nặng).

> Chênh lệch **hợp lệ**, đừng tưởng là lỗi: BC006 dư cuối Nợ (`165.173.074.702`) thấp hơn BC005 tổng
> tài sản (`165.309.773.349`) đúng `136.698.647`. BC006 bù trừ Nợ/Có trong cùng tài khoản, còn CĐKT
> phải **tách tài khoản lưỡng tính theo từng đối tượng**. Nên CĐKT luôn ≥ và chênh đúng phần tách ra.

---

## 6. Thời gian chạy thật — để biết thế nào là bất thường

Đo trên `IACC_CHULONG` qua internet, kỳ 1 tháng:

| Báo cáo | Thời gian | | Báo cáo | Thời gian |
|---|---:|---|---|---:|
| BC011 LCTT Chú Long | 114–150 s | | BC009/BC010 | 10 s |
| BC005 CĐKT | 103–117 s | | BC007 | 8 s |
| BC002 KQKD công việc | 31 s | | BC013 | 8 s |
| BC006 CĐ phát sinh | 28 s | | BC012 | 1 s |
| BC001 KQKD tháng | 25 s | | BC008 / BC014 | < 1 s |

BC005 và BC011 nặng vì phải dựng lại số dư luỹ kế **từ 01/01** (do `BALANCE_VIEW` trống);
BC011 còn gọi chính engine của BC005 rồi quét LEDGER thêm lần nữa.

BC002 có **một lần** đo 692 s rồi hai lần sau 31 s — biến động phía SQL Server, chưa truy ra nguyên
nhân. Nút thắt gốc: DB 10,6 GB / buffer pool 1.410 MB (trần cứng SQL Express) ≈ 7,7 : 1.
`AUTO_SHRINK` và `AUTO_CLOSE` đã được tắt sẵn — kiểm rồi, không phải thủ phạm.
